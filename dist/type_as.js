"use strict";
let username;
username = "gotcha";
// (<string>username).toUpperCase(); // แปลงเป็น string ก่อนแล้วค่อยเรียก method toUpperCase
username.toUpperCase(); // แปลงเป็น string ก่อนแล้วค่อยเรียก method toUpperCase
console.log(username); // gotcha
